name: Kernel Build

on: 
  workflow_dispatch: 

  push:
    branches: [ main ]
    tags: [ 'v*' ]

jobs: 
  build:
    name:  Build Kernel
    runs-on: ubuntu-latest

    strategy:  
      fail-fast: false
      matrix:
        model: [beyond0lte, beyond1lte, beyond2lte, beyondx, d1, d1x,d1xks, d2s, d2x, d2xks]
        variant: [ksu, susfs, vanilla]

    steps: 
      # (1) Checkout repository without submodules
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # (2) Install build dependencies
      - name: Set up build environment
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            git-core gnupg flex bison build-essential zip curl \
            zlib1g-dev libc6-dev-i386 x11proto-core-dev \
            libx11-dev lib32z1-dev libgl1-mesa-dev \
            libxml2-utils xsltproc unzip fontconfig cpio

      # (3) Set build flags by variant
      - name: Set build flags
        id: vars
        run:  |
          case "${{ matrix.variant }}" in
            ksu)
              echo "FLAGS=-k y" >> $GITHUB_OUTPUT
              ;;
            susfs)
              echo "FLAGS=-k y -s y" >> $GITHUB_OUTPUT
              ;;
            vanilla)
              echo "FLAGS=-k n" >> $GITHUB_OUTPUT
              ;;
          esac

      # (4) Build kernel
      - name: Run kernel build
        run: |
          chmod +x ./build.sh
          ./build.sh -m ${{ matrix.model }} ${{ steps.vars.outputs.FLAGS }}

      # (5) Extract and prepare artifact
      - name:  Prepare artifact
        id: artifact_name
        run: |
          ZIP_FILE=$(ls build/out/${{ matrix.model }}/*.zip | head -n 1)
          ARTIFACT_NAME=$(basename "$ZIP_FILE" .zip)
          
          # Unzip to a temporary directory
          mkdir -p "artifact-content/$ARTIFACT_NAME"
          unzip -q "$ZIP_FILE" -d "artifact-content/$ARTIFACT_NAME"
          
          echo "name=$ARTIFACT_NAME" >> $GITHUB_OUTPUT
          echo "Prepared artifact: $ARTIFACT_NAME"

      # (6) Upload kernel artifact
      - name: Upload kernel artifact
        uses:  actions/upload-artifact@v4
        with:
          name:  ${{ steps.artifact_name.outputs.name }}
          path: artifact-content/${{ steps.artifact_name.outputs.name }}/*

  release:
    name: Publish GitHub Release
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: ubuntu-latest

    steps: 
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with: 
          pattern: ExtremeKRNL-*
          path: kernel-artifacts

      - name: Generate release notes
        run:  |
          cat > release.md <<EOF
          Kernel Information
          - Kernel Version: Linux 4.14.356
          - SuSFS Version: 2.0.0
          - RKSU main Version: 32391
          - RKSU susfs-rksu-master Version: 32359
          - Build Date: $(date +%Y-%m-%d)

          Supported Devices
          - Galaxy S10e (beyond0lte)
          - Galaxy S10 (beyond1lte)
          - Galaxy S10+ (beyond2lte)
          - Galaxy S10 5G (beyondx)
          - Galaxy Note10 (d1)
          - Galaxy Note10 5G (d1x)
          - Galaxy Note10 5G Korea (d1xks)
          - Galaxy Note10+ (d2s)
          - Galaxy Note10+ 5G (d2x)
          - Galaxy Note10+ 5G Korea (d2xks)

          This release has 3 flavors per device model:

          1st is the RKSU Variant.
          2nd is the RKSU + SuSFS variant.
          3rd is the Vanilla build, no RKSU.

          Build Notes
          - Built via GitHub Actions
          - Total builds: 30 (10 models x 3 variants)
          EOF

      - name:  Repackage for release
        run: |
          mkdir -p release-files
          
          # Repackage each artifact
          for artifact_dir in kernel-artifacts/ExtremeKRNL-*/; do
            ARTIFACT_NAME=$(basename "$artifact_dir")
            echo "Packaging $ARTIFACT_NAME..."
            cd "$artifact_dir"
            zip -r "../../release-files/${ARTIFACT_NAME}.zip" .
            cd ../..
          done
          
          echo "Release files:"
          ls -lh release-files/

      - name:  Create GitHub Release
        uses: softprops/action-gh-release@v2
        with: 
          tag_name: ${{ github.ref_name }}
          name: ExtremeKRNL ${{ github.ref_name }}
          body_path: release.md
          files: release-files/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}